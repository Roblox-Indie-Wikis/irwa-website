name: Minify JS
on:
  push:
    paths:
      - 'scripts.js' # Only trigger when scripts.js changes

permissions:
  contents: write
  pull-requests: write

jobs:
  minify-and-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Minifier
        run: npm install -g terser

      - name: Minify JavaScript
        run: |
          # Directly target scripts.js and output to scripts.min.js
          terser scripts.js -o scripts.min.js

      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "style: minify scripts.js"
          committer: GitHub <noreply@github.com>
          author: github-actions[bot] <github-actions[bot]@users.noreply.github.com>
          branch: patch/sync-scripts-js
          title: "Update Minified scripts.js"
          body: "Automated PR to update scripts.min.js"
          delete-branch: true

      - name: Auto-merge PR
        if: steps.cpr.outputs.pull-request-operation == 'created'
        run: gh pr merge "${{ steps.cpr.outputs.pull-request-number }}" --merge --admin
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}